.venv/
venv/
__pycache__/
*.pyc
*.log
build/
dist/

# MSIX-sandboxed dev-tool workaround, not needed by a normal uv install.
# Kept on disk for local use, just never published.
repair-env.bat

# Installer build output (PyInstaller's dist/ above already covers the
# PyInstaller side; this is Inno Setup's own default output folder).
installer/Output/

# Local archive of superseded installer builds -- never published or tracked.
outputs/

# Local project handoff file - never commit or release
CLAUDE.md

AGENTS.md

# Local release-note preview output. The changelog remains the publishable source.
release-notes.md
